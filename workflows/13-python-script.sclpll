@workflow python-script "Run an ordinary Python script as a workflow step"

@step source
  let [{"id": 1, "amount": 12.5}, {"id": 2, "amount": 8.0}]

@step scored
  python "scorer" args=["--multiplier", "2"] input=@source

@step verified
  assert_schema @scored {"id": "integer", "score": "number"}
