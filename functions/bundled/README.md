# Bundled generic functions

This directory mirrors the generic-function source currently included in the core `sclpl` package. It is here for discovery, review, and contribution planning only.

Install SCLPL itself to use these functions. Do not install this directory as a separate package or copy it into a plugin; that would create a second registry and risk name collisions.
