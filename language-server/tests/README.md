# Language-server compatibility tests

These tests exercise this package against the public parser, formatter, registry, and project APIs from `sclpl`. They protect the editor-facing contract without duplicating DSL semantics.
